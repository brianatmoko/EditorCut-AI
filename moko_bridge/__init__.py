"""MOKO OS bridge for OpenCut AI auto-editor.

Menggunakan sistem API MOKO OS: memanggil free API gateways
(opencode, omniroute, ninerouter) dengan format OpenAI-compatible.
Tidak menggunakan local LLM (GGUF).
"""
